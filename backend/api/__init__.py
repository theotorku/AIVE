"""FastAPI app for the ABI dashboard (Goal 04).

Presentation + workflow only: it wraps the existing pipeline and serves the
real artifacts (profile.json / abi_score.json / pipeline.json / benchmark). It
does not compute or mock any ABI data, and it does not change scoring formulas
or extraction contracts.
"""
